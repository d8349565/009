# 项目未被实际使用的内容梳理

> 本文件梳理了当前以 `gui_app_pyqt6.py` 为唯一入口时，项目中未被实际调用或冗余的代码、脚本、依赖和文档。

---

## 1. 未被 GUI 入口直接用到的内容

### （1）命令行入口/脚本
- `main.py` 的命令行交互（模式选择、input、print 等）
- `run_comprehensive.py`、`run.bat` 等批处理脚本

### （2）命令行工具/测试脚本
- `demo_llm_providers.py`（LLM provider 测试）
- `report_search.py`（报告检索工具）

### （3）文档/说明
- `README.md`
- `TECHNICAL_STACK_ANALYSIS.md`
- `项目数据传递逻辑说明.md`

### （4）辅助工具
- `performance_timer.py`、`process_logger.py`（命令行下详细日志，GUI 只用部分功能）
- `report_metadata.py` 的 CLI 检索功能

### （5）部分配置/环境
- `.env` 和 `config/runtime.json` 仍被 GUI 用于配置，但命令行下的环境变量注入方式不会被 GUI 直接用到

---

## 2. 代码层面冗余/未用到的内容

### （1）main.py 的命令行交互
- 所有 input、print、模式选择、命令行参数解析等

### （2）agent/LLM provider 的测试代码
- `demo_llm_providers.py`、`agent_prompts.py` 的部分测试用 prompt

### （3）search_engine.py 的命令行调试输出
- 详细 print 日志、异常追踪，GUI 下只保留核心日志

### （4）report_metadata.py 的 CLI 检索功能
- GUI 有自己的历史报告管理

---

## 3. 依赖层面冗余

- requirements.txt 中部分包（如 tqdm、rich、typer）只用于命令行交互，GUI 不会用到

---

## 4. 总结与建议

- CLI 入口、脚本、命令行工具可移至 `tools/` 或 `cli/` 文件夹，主项目只保留 GUI 相关代码
- requirements.txt 可拆分为 `requirements-gui.txt` 和 `requirements-cli.txt`
- 文档类文件可归档至 `docs/`

---

如需更细致的“未被调用代码”分析（如函数/类级别），可进一步指定需求。
