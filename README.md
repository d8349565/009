# 🔬 AI研究报告生成系统 (AI Research Report Generator)

一个基于 DeepSeek 等大模型的智能信息研究和报告生成系统的 Windows 桌面应用。它能够自动搜索、分析、整理信息，并生成结构化的专业研究报告。

---

## ✨ 核心特性

*   **🤖 智能多 Agent 协作**: 需求分析、信息收集、报告撰写、质量评审、综合分析 5 个专业 Agent 分工协作。
*   **🧠 多 LLM 供应商支持**: 灵活配置 DeepSeek (高性价比)、智谱 GLM (中文优化)、OpenRouter (聚合模型) 等不同供应商，实现成本与质量的最佳平衡。
*   **🖥️ 现代化 GUI 界面**: 基于 wxPython 的桌面图形界面，操作直观，实时显示任务日志和进度。
*   **📚 综合报告生成**: 支持整合多个历史报告，进行跨文档的数据交叉验证、矛盾识别和新洞察发现。
*   **🔍 灵活搜索模式**:
    *   **快速模式**: 单次搜索，约 30 秒出结果。
    *   **完整模式**: 多轮迭代搜索与评估，约 2-3 分钟，质量更高。
*   **🛡️ 鲁棒性设计**: 自动处理网络错误、API 限流重试、内容截断防止 Token 超限。
*   **💾 完整的报告管理**: 自动保存 Markdown 和元数据，支持历史记录检索、查看和管理。

---

## 🚀 快速开始

### 环境要求
*   Windows 10/11
*   Python 3.8+

### 安装依赖
```bash
pip install -r requirements.txt
```

### 配置 API 密钥
复制 `.env.example` 为 `.env`，并在其中填入你的 API 密钥：
```ini
DEEPSEEK_API_KEY=sk-xxxxxxxx
ZHIPU_API_KEY=xxxxxxxx (可选)
TAVILY_API_KEY=tvly-xxxxxxxx (可选，用于云端搜索)
```

### 启动应用
**方式 1 (推荐)**: 双击根目录下的 `启动GUI.bat`。

**方式 2**: 命令行运行
```bash
python gui_app.py
```

---

## 📖 使用指南

### 1. 新建报告
1.  进入 **"📝 新建报告"** 标签页。
2.  输入研究主题（如："2024年中国人形机器人产业链分析"）。
3.  选择 **搜索模式**（快速/完整）。
4.  点击 **🚀 开始生成**。
5.  系统会自动进行网页搜索、评估来源、撰写报告，完成后会自动打开生成的 Markdown 文件。

### 2. 综合报告
1.  确保已有相关的历史单篇报告。
2.  进入 **"📚 综合报告"** 标签页。
3.  输入综合主题（如："人工智能行业年度综合分析"）。
4.  在列表中勾选需要整合的子报告。
5.  点击 **🚀 生成综合报告**，AI 将提取各报告的关键数据进行对比和深度分析。

### 3. 系统配置
在 **"⚙️ 系统配置"** 标签页中，你可以可视化的管理：
*   **Agent 模型**: 为每个角色指定不同的模型（如让"报告撰写员"使用 DeepSeek-Reasoner 推理模型）。
*   **API 密钥**: 安全地更新你的 Keys。
*   **搜索设置**: 切换 SearXNG (本地) 或 Tavily (云端)，调整并发数和内容长度。

---

## 📂 项目结构

```
009/
├── gui_app.py                 # GUI 主程序入口
├── main.py                    # 命令行入口 & 核心逻辑编排
├── agents.py                  # Agent 类定义 (分析师, 收集员, 撰写员等)
├── search_engine.py           # 搜索引擎封装 (SearXNG, Tavily)
├── llm_providers.py           # 多 LLM 供应商适配层
├── document_parser.py         # 文档解析器
├── reports/                   # 报告生成目录 (.md, .json)
├── config.py                  # 配置加载
└── model_config.json          # 模型预设配置
```

---

## ❓ 常见问题

**Q: 启动时提示 `WSALookupServiceBegin failed`?**
A: 这是 Windows 网络通知服务的常见警告，不影响程序功能，可忽略。

**Q: 搜索无结果?**
A: 请检查网络连接，或在配置中切换搜索引擎 (推荐先配置 Tavily API Key 以获得稳定体验)。

**Q: 如何降低 API 成本?**
A: 在配置中将"需求分析"和"质量评审"等轻量任务切换为 `glm-4-flash` 或 `deepseek-chat`，仅在"撰写"环节使用推理模型。

---

## 📄 许可证

MIT License
